application.title=Jsettlers
application.vendor=szityu
build.classes.dir=${build.dir}/classes
build.classes.excludes=**/*.java,**/*.form
# This directory is removed when the project is cleaned:
build.dir=build
build.generated.dir=${build.dir}/generated
# Only compile against the classpath explicitly listed here:
build.sysclasspath=ignore
build.test.classes.dir=${build.dir}/test/classes
build.test.results.dir=${build.dir}/test/results
debug.classpath=\
    ${run.classpath}
debug.test.classpath=\
    ${run.test.classpath}
# This directory is removed when the project is cleaned:
dist.dir=dist
dist.jar=${dist.dir}/Jsettlers.jar
dist.javadoc.dir=${dist.dir}/javadoc
excludes=
file.reference.projekt_x42__Settlers_-java=.
file.reference.SmartSettlers.jar=../SmartSettlers/dist/SmartSettlers.jar
file.reference.src-java=../games/jsettlers-1.0.6-src/src/java
includes=**
jar.compress=false
javac.classpath=\
    ${file.reference.SmartSettlers.jar}
# Space-separated list of extra javac options
javac.compilerargs=-cp "C:/_szityu/projekt x42 (Settlers)/SmartSettlers/dist/SmartSettlers.jar"
javac.deprecation=false
javac.source=1.5
javac.target=1.5
javac.test.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}:\
    ${libs.junit.classpath}:\
    ${libs.junit_4.classpath}
javadoc.additionalparam=
javadoc.author=false
javadoc.encoding=${source.encoding}
javadoc.noindex=false
javadoc.nonavbar=false
javadoc.notree=false
javadoc.private=false
javadoc.splitindex=true
javadoc.use=true
javadoc.version=false
javadoc.windowtitle=
main.class=experiment.Experiment
manifest.file=manifest.mf
meta.inf.dir=${src.dir}/META-INF
platform.active=default_platform
run.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}
run.jvmargs=-cp "C:/_szityu/projekt x42 (Settlers)/SmartSettlers/dist/SmartSettlers.jar" -Xms128m -Xmx1024m
run.test.classpath=\
    ${javac.test.classpath}:\
    ${build.test.classes.dir}
source.encoding=UTF-8
src.java.dir=src\\java
